# Changelog

## v1.0.0 (2026-05-22)

- Initial release
- Sidebar view with file list and sync status
- Sync notes to IMA via `import_doc` (first sync) and `append_doc` (update)
- Add notes to IMA knowledge base via `add_knowledge`
- Batch select and sync files
- Auto-sync on save (configurable)
- Settings panel with credential management
- Test connection and knowledge base discovery
